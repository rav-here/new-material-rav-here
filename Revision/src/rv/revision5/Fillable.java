package rv.revision5;
// interface fillable extends interface drawable
public interface Fillable extends Drawable {
	
	// abstract methods of interface that MUST be 
	// implemented by classes that implement this interface
	void fillingColour(String colour);
	void size(String size);

}
