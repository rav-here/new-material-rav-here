package rv.revision5;
// interface drawable 
public interface Drawable {

	// abstract methods of interface that MUST be 
	// implemented by classes that implement this interface
	void drawingColour(String colour);
	void thickness(int thickness);
}
